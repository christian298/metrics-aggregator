# metrics-aggregator

Frontend Metrics Aggregator

## Start

1. Start Grafana and Influx  
   - `make dc-up`
2. Start server which exposes `/metrics` endpoint 
   - `make run`

## Grafana
`http://localhost:3000`